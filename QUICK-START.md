# البدء السريع

## 1. تشغيل المشروع محلياً

```bash
cd dental-inspection-app
npm install
npm run dev
```

افتح: http://localhost:3000

---

## 2. إعداد Supabase (مرة واحدة فقط)

### الخطوات:

1. **إنشاء حساب:**
   - اذهب إلى https://supabase.com
   - سجل حساب جديد (مجاني)

2. **إنشاء مشروع:**
   - اضغط "New Project"
   - اسم المشروع: `dental-inspection`
   - كلمة المرور: احفظها!
   - المنطقة: اختر الأقرب

3. **تشغيل قاعدة البيانات:**
   - اذهب إلى **SQL Editor**
   - افتح ملف: `supabase/migrations/001_initial_schema.sql`
   - انسخ كل المحتوى
   - الصق في SQL Editor
   - اضغط **Run** ✅

4. **نسخ المفاتيح:**
   - اذهب إلى **Settings** → **API**
   - انسخ:
     - Project URL
     - anon public key
     - service_role key

5. **إضافة المفاتيح للمشروع:**
   ```bash
   # أنشئ ملف .env.local
   cp .env.local.example .env.local
   ```

   افتح `.env.local` وضع المفاتيح:
   ```
   NEXT_PUBLIC_SUPABASE_URL=رابط_المشروع
   NEXT_PUBLIC_SUPABASE_ANON_KEY=المفتاح_العام
   SUPABASE_SERVICE_ROLE_KEY=المفتاح_الخاص
   ```

---

## 3. إضافة أول مستخدم (مسؤول)

### أ) تشفير كلمة المرور:

1. اذهب إلى: https://bcrypt-generator.com
2. أدخل كلمة المرور (مثلاً: `123456`)
3. اضغط "Encrypt"
4. انسخ النتيجة

### ب) إضافة المستخدم في Supabase:

1. اذهب إلى **Table Editor** → **users**
2. اضغط **Insert row**
3. املأ البيانات:
   ```
   email: admin@moh.gov.sa
   password_hash: (الصق النتيجة من الخطوة السابقة)
   role: admin
   full_name: المسؤول الرئيسي
   phone: 0501234567
   is_active: ✓
   ```
4. اضغط **Save**

---

## 4. تسجيل الدخول

1. افتح التطبيق: http://localhost:3000
2. سجل الدخول:
   - البريد: `admin@moh.gov.sa`
   - كلمة المرور: `123456` (أو التي اخترتها)
3. ستدخل لوحة المسؤول! 🎉

---

## 5. المعاينة على الهاتف

### أثناء التطوير:

1. احصل على IP الكمبيوتر:
   ```bash
   # Windows
   ipconfig

   # Mac/Linux
   ifconfig
   ```

2. ابحث عن IPv4 (مثلاً: `192.168.1.100`)

3. على الهاتف (نفس الواي فاي):
   - افتح: `http://192.168.1.100:3000`

---

## 6. النشر على Vercel (للمشاركة)

### الخطوات:

1. **رفع على GitHub:**
   ```bash
   # أنشئ repository على github.com
   git remote add origin https://github.com/username/repo.git
   git branch -M main
   git push -u origin main
   ```

2. **ربط Vercel:**
   - اذهب إلى https://vercel.com
   - سجل دخول بـ GitHub
   - Import Project
   - اختر repository
   - أضف Environment Variables (نفس .env.local)
   - Deploy!

3. **المشاركة:**
   - انسخ الرابط: `https://your-app.vercel.app`
   - شاركه مع الزملاء
   - يفتحونه ويضيفونه للشاشة الرئيسية!

---

## 7. حل المشاكل الشائعة

### المشكلة: "Invalid API key"
**الحل:** تحقق من ملف `.env.local` - المفاتيح صحيحة؟

### المشكلة: "Table does not exist"
**الحل:** شغّل SQL migration في Supabase SQL Editor

### المشكلة: "Cannot connect to Supabase"
**الحل:** تأكد من رابط المشروع صحيح في `.env.local`

### المشكلة: "Login failed"
**الحل:** تأكد من إضافة المستخدم في جدول users

---

## الخطوات التالية

بعد تشغيل التطبيق بنجاح:

1. ✅ إضافة المنشآت
2. ✅ إنشاء المهام
3. ✅ إضافة مفتشين
4. ✅ تخصيص النماذج

**محتاج مساعدة؟** راجع ملف README.md الكامل!
